# Used automatically by `vite build` (production mode).
# This guarantees the production bundle always points at the deployed
# backend, even if someone runs `npm run build` locally instead of
# letting Netlify build it (which is what caused the localhost bug).
VITE_API_URL=https://tarang-api.onrender.com/api
